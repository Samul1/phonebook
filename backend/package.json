{
  "name": "backend",
  "version": "0.0.1",
  "description": "puhelinluettelo app:n backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "git add . && git commit -m deploy && git push"
  },
  "author": "Samul1",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.16.0",
    "morgan": "^1.10.0"
  }
}
